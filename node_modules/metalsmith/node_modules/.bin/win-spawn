#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../win-fork/bin/win-spawn" "$@"
  ret=$?
else 
  node  "$basedir/../win-fork/bin/win-spawn" "$@"
  ret=$?
fi
exit $ret
