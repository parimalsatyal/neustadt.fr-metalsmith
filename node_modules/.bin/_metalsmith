#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../metalsmith/bin/_metalsmith" "$@"
  ret=$?
else 
  node  "$basedir/../metalsmith/bin/_metalsmith" "$@"
  ret=$?
fi
exit $ret
